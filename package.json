{
  "name": "juniorcars-next-cms",
  "version": "1.0.0",
  "description": "JuniorCars - Full-stack Next.js application with a custom CMS",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "install": "cd frontend && npm install",
    "lint": "cd frontend && npm run lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}